# Permutation: Parallel block + Let binding + Context (25x14x17)
# Tests: Merging results from parallel into single context

parallel:
  let result_a = session "Compute A"
  let result_b = session "Compute B"
  let result_c = session "Compute C"

session:
  prompt: "Merge all results"
  context: [result_a, result_b, result_c]
