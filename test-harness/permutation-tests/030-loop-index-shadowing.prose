# Permutation: For-each loop + For-each loop (28x28)
# Tests: Same index variable name in nested loops

let outer = session "Generate outer list"

for item in outer:
  let inner = session "Generate inner for {item}"
  for item in inner:
    session "Inner item: {item}"
