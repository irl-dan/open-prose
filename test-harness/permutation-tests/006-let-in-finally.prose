# Permutation: Let binding + Try/catch (14x32)
# Tests: Variable defined in finally block

try:
  session "Main operation"
catch:
  session "Handle error"
finally:
  let cleanup = session "Cleanup resources"

session "Cleanup was: {cleanup}"
